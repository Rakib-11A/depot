<%= turbo_stream.replace 'notice' do %>
  <%= render partial: 'layouts/notice', locals: { notice: "Item is added successfully." } %>
<% end %>

<%= turbo_stream.replace 'cart' do %>
  <%= render partial: 'layouts/cart', locals: {cart: @cart} %>
<% end %>